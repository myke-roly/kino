import { StyleSheet } from 'react-native';

export const stylesGlobal = StyleSheet.create({
  container: {
    padding: 10,
    flex: 1,
    justifyContent: 'center',
    alignItems: "center"
  },
});
